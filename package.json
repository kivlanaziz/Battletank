{
  "name": "BattleTank",
  "version": "1.0.0",
  "description": "BattleCity Clone",
  "main": "server.js",
  "repository": "git@github.com:kivlanaziz/Battletank.git",
  "author": "Naufal <nmuntaaza.wh@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "nodemon": "^1.19.4"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "auto-reload": "browser-sync start --proxy=localhost:8080 --files='**/*.css, **/*.pug, **/*.js, **/*.html' --ignore=node_modules --reload-delay 10 --no-ui --no-notify"
  }
}
